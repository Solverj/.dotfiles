git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm
ctrl + space + I in tmux to install plugins
Continuum auto-saves/restores sessions (every ~15 min); just leave tmux running and it will come back on next login.
Thumbs lets you press `prefix + f` to fuzzy-highlight URLs, hashes, and paths in the active pane; hit `<Enter>` to copy, or press an uppercase hint (Shift+letter) to open the target via `xdg-open`.
